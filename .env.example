# =============================================================================
# SixMarket Environment Configuration
# =============================================================================
# Copy to .env:  cp .env.example .env
#
# QUICK START (Local Docker):
#   1. docker compose -f docker-compose.dev.yml up -d
#   2. cp .env.example .env
#   3. npx prisma migrate dev && npm run seed
#   4. npm run dev
#
# These defaults work out of the box with docker-compose.dev.yml
# =============================================================================

# -----------------------------------------------------------------------------
# Auth (Google OAuth)
# -----------------------------------------------------------------------------
# Get credentials at: https://console.cloud.google.com/apis/credentials
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
NEXTAUTH_URL=http://localhost:3003
JWT_SECRET=dev-secret-change-in-production

# -----------------------------------------------------------------------------
# Database (PostgreSQL)
# -----------------------------------------------------------------------------
# Local Docker (default):
DATABASE_URL=postgresql://sixmarket:sixmarket@localhost:5432/sixmarket
DIRECT_URL=postgresql://sixmarket:sixmarket@localhost:5432/sixmarket

# Docker production (docker-compose.yml) - change localhost → postgres:
# DATABASE_URL=postgresql://sixmarket:sixmarket@postgres:5432/sixmarket
# DIRECT_URL=postgresql://sixmarket:sixmarket@postgres:5432/sixmarket

# Cloud (Supabase, Neon, etc.) - use your connection string:
# DATABASE_URL=postgresql://user:pass@host:5432/db?pgbouncer=true
# DIRECT_URL=postgresql://user:pass@host:5432/db

PRISMA_ENGINE_PROTOCOL=json

# -----------------------------------------------------------------------------
# Storage (S3 / MinIO)
# -----------------------------------------------------------------------------
# Local Docker with MinIO (default):
AWS_BUCKET_NAME=sixmarket
AWS_BUCKET_REGION=us-east-1
AWS_ACCESS_KEY_ID=minioadmin
AWS_SECRET_ACCESS_KEY=minioadmin
AWS_ENDPOINT_URL=http://localhost:9000
AWS_FORCE_PATH_STYLE=true

# Docker production - change localhost → minio:
# AWS_ENDPOINT_URL=http://minio:9000

# AWS S3 - remove ENDPOINT_URL and FORCE_PATH_STYLE, use real credentials:
# AWS_BUCKET_NAME=your-bucket
# AWS_BUCKET_REGION=us-east-1
# AWS_ACCESS_KEY_ID=AKIA...
# AWS_SECRET_ACCESS_KEY=...
# AWS_ENDPOINT_URL=
# AWS_FORCE_PATH_STYLE=

# -----------------------------------------------------------------------------
# Redis (Rate Limiting)
# -----------------------------------------------------------------------------
# Cloud (Upstash):
UPSTASH_REDIS_REST_URL=
UPSTASH_REDIS_REST_TOKEN=

# Local Redis runs on localhost:6379 via docker-compose.dev.yml
# (Upstash SDK doesn't support local Redis directly)

# -----------------------------------------------------------------------------
# Docker Compose Settings (only used by docker-compose.yml)
# -----------------------------------------------------------------------------
POSTGRES_PASSWORD=sixmarket
MINIO_ROOT_USER=minioadmin
MINIO_ROOT_PASSWORD=minioadmin
